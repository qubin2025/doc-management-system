# 全过程工程咨询管理系统 v2.5.0 — 部署说明

## 一、配置 DASHSCOPE_API_KEY

**文件路径：** `backend/.env` 第9行

```
DASHSCOPE_API_KEY=sk-your-dashscope-api-key  ← 替换为真实Key
```

**获取地址：** https://dashscope.console.aliyun.com/apiKey

**用途：** 通义Embedding 768维向量嵌入，用于语义搜索和混合检索。未配置时自动降级为哈希向量。

## 二、完整环境变量清单

### backend/.env

| 变量 | 必填 | 说明 | 获取地址 |
|------|------|------|----------|
| DEEPSEEK_API_KEY | **是** | AI审查/方案生成 | platform.deepseek.com |
| DASHSCOPE_API_KEY | 推荐 | 语义搜索向量嵌入 | dashscope.console.aliyun.com |
| JWT_SECRET | **是** | Session加密 | `openssl rand -hex 32` |
| ZHIPU_API_KEY | 可选 | GLM-4V图片 | open.bigmodel.cn |
| QWEN_API_KEY | 可选 | 千问视觉 | dashscope.console.aliyun.com |

### Python LightRAG服务（环境变量或docker-compose注入）

| 变量 | 必填 | 说明 |
|------|------|------|
| DEEPSEEK_API_KEY | 推荐 | AI实体抽取 |
| DASHSCOPE_API_KEY | 推荐 | 向量嵌入 |
| LIGHTRAG_PORT | 否 | 默认8000 |

## 三、部署方式

### 方式一：Docker Compose（生产推荐）

```bash
cp backend/.env.example backend/.env
vim backend/.env  # 填入 Key

export DEEPSEEK_API_KEY=sk-xxx
export DASHSCOPE_API_KEY=sk-xxx
docker compose -p doc-mgmt build
docker compose -p doc-mgmt up -d
```

服务：Nginx(80) + 后端(3000) + Neo4j(7687) + LightRAG(8000)

### 方式二：PM2 + Nginx（Linux生产）

```bash
# 后端
cd backend && npm install && pm2 start server.js --name doc-api

# LightRAG（可选）
cd services/lightrag-server && pip install -r requirements.txt
DEEPSEEK_API_KEY=sk-xxx pm2 start main.py --name doc-rag --interpreter python3

# 前端
cd ../.. && npm run build
sudo cp deploy/nginx-docker.conf /etc/nginx/conf.d/doc-mgmt.conf
sudo nginx -t && sudo systemctl reload nginx
```

### 方式三：开发模式

```bash
cd backend && npm install && node server.js    # 后端 :3000
npm install && npm run dev                       # 前端 :5173

# Python LightRAG（可选）
cd services/lightrag-server && pip install -r requirements.txt
DEEPSEEK_API_KEY=sk-xxx python main.py           # :8000
```

## 四、部署后验证

```bash
# 健康检查
curl http://localhost:3000/api | grep ok

# 运行测试（需后端运行中）
npx vitest run
```

## 五、默认账号

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | admin123 | 管理员 |
| pm_wang | pm123456 | 项目经理 |
| constructor_li | constr123 | 建设单位 |
| viewer_zhang | viewer123 | 查看者 |
