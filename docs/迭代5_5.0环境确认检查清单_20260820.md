# 迭代5 · 5.0 环境确认检查清单

**生成日期**：2026-08-20
**目标**：在编写 kbWorker.js 之前，确认嵌入服务接口格式、SQLite 现状、队列任务表字段、文档解析依赖，避免代码返工

---

## 一、需检查的具体字段与预期返回值

### 1.1 嵌入服务 `/api/ai/embed`（前端当前实现）

| 检查项 | 预期值 | 实测值 |
|--------|--------|--------|
| 后端是否有 `/ai/embed` 路由 | 应有 `router.post('/embed', ...)` | ❌ **后端 ai.js 无此端点** |
| 前端调用方式 | `POST /api/ai/embed` body: `{ text, textType }` | ✅ 见 api.ts:517 |
| 响应格式 | `{ embedding: number[] }` | ✅ 见 api.ts:521 |
| 失败回退 | 直接调 DashScope `text-embedding-v1` | ✅ 见 api.ts:531-542 |
| DashScope 请求体 | `{ model, input:{texts:[text]}, parameters:{text_type} }` | ✅ |
| DashScope 响应路径 | `data.output.embeddings[0].embedding` | ✅ |
| 超时设置 | 前端 20s（后端代理）/ 30s（直连） | ✅ |
| API Key 来源 | `process.env.DASHSCOPE_API_KEY` / `VITE_QWEN_API_KEY` | ✅ backend/.env 已配置 |

**结论**：后端 Worker 应直接调用 DashScope（复用 backend/.env 的 DASHSCOPE_API_KEY），不再走 /api/ai/embed（因为该端点不存在）。需新建 `backend/services/embeddingService.js` 封装批量嵌入。

### 1.2 批量嵌入能力（待压测）

| 检查项 | 预期 | 实测 |
|--------|------|------|
| DashScope 批量接口 | `input.texts` 支持数组（最多 25 条） | 待测 |
| 单批最大文本长度 | 2048 tokens | 待测 |
| 响应时间（单条） | < 500ms | 待测 |
| 响应时间（10 条批量） | < 2s | 待测 |
| 并发限制 | DashScope QPS 限制 | 待测 |

### 1.3 SQLite 现状

| 检查项 | 预期 | 实测 |
|--------|------|------|
| DB 文件路径 | `backend/data/planning.db` | ✅ |
| 队列表名 | `kb_sync_queue` | ✅ db.js:334 |
| 队列表字段 | id/project_name/source/record_id/action/status/priority/retry_count/max_retries/error_msg/created_at/processed_at | ✅ db.js:335-346 |
| 队列索引 | idx_kb_sync_queue_status / _project / _pending | ✅ db.js:348-350 |
| project_members 表 | 已存在（迭代4） | ✅ db.js:353+ |
| vector_embeddings 表 | 应不存在（5.1 新建） | 待测 |
| 现有表总数 | 21 张（CLAUDE.md 记录） | 待测 |

### 1.4 队列任务表字段定义（kb_sync_queue）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 主键 |
| project_name | TEXT | NOT NULL | 项目名 |
| source | TEXT | NOT NULL CHECK in ('daily','issue','experience','document') | 来源 |
| record_id | TEXT | NOT NULL | 业务记录 ID |
| action | TEXT | DEFAULT 'upsert' CHECK in ('upsert','delete') | 操作 |
| status | TEXT | DEFAULT 'pending' CHECK in ('pending','processing','done','failed') | 状态 |
| priority | INTEGER | DEFAULT 0 | 优先级（DESC） |
| retry_count | INTEGER | DEFAULT 0 | 已重试次数 |
| max_retries | INTEGER | DEFAULT 3 | 最大重试 |
| error_msg | TEXT | | 错误信息 |
| created_at | TEXT | DEFAULT datetime('now') | 创建时间 |
| processed_at | TEXT | | 处理时间 |

**注意**：当前表无 `locked_by` / `locked_at` 字段，5.4 任务锁需新增。

### 1.5 文档解析依赖（前端 vs 后端）

| 模块 | 前端依赖 | 后端替代方案 | 状态 |
|------|---------|-------------|------|
| PDF | pdfjs-dist（Web Worker） | pdfjs-dist 的 node 入口 / pdf-parse | 待选型 |
| DOCX | mammoth（浏览器版） | mammoth（Node 版可用） | ✅ 可复用 |
| XLSX | xlsx | xlsx（Node 可用） | ✅ 可复用 |
| OCR | paddleocr-server :8001 | 已是后端服务，可复用 | ✅ 可复用 |
| 纯文本/Markdown | 无依赖 | 无依赖 | ✅ |

**结论**：`documentParser.ts` 不能直接搬到后端，需新建 `backend/services/docParser.js`，PDF 用 pdf-parse，DOCX/XLSX 复用 mammoth/xlsx，OCR 调 paddleocr-server。

### 1.6 chunker 策略（前端 src/data/chunker.ts）

| 策略 | 说明 | 后端复用 |
|------|------|---------|
| 固定窗口 | 按字符数分块（默认 500 字符） | ✅ 可直接搬 |
| 语义分块 | 按段落/标题分块 | ✅ 可直接搬 |
| 表格分块 | 表格整体保留 | ✅ 可直接搬 |

**结论**：chunker 是纯 TypeScript 无浏览器依赖，可直接转为 Node.js 模块。

---

## 二、调研脚本设计

### 2.1 脚本目标
1. 检测 SQLite 所有表名+字段
2. 检测 kb_sync_queue 表结构与索引
3. 测试 DashScope 单条嵌入响应
4. 测试 DashScope 批量嵌入响应（10 条）
5. 输出 JSON 报告

### 2.2 预期输出字段
```json
{
  "sqlite": {
    "dbPath": "backend/data/planning.db",
    "tables": [{ "name": "...", "columns": [...] }],
    "kb_sync_queue": { "columns": [...], "indexes": [...] },
    "vector_embeddings_exists": false
  },
  "embedding": {
    "single": { "status": "ok", "dim": 1536, "durationMs": 480, "sample": [0.01, ...] },
    "batch": { "status": "ok", "count": 10, "durationMs": 1200, "dim": 1536 }
  },
  "conclusion": {
    "embedStrategy": "direct_dashscope",
    "vectorStoreStrategy": "sqlite_table",
    "docParserStrategy": "new_node_module",
    "chunkerStrategy": "reuse_frontend"
  }
}
```

---

## 三、风险提示

1. **DashScope 批量接口**：需确认 `input.texts` 是否支持数组（文档说是，实测待验证）
2. **向量维度**：DashScope `text-embedding-v1` 维度 1536，需在 vector_embeddings.dimension 字段记录
3. **PDF 解析**：pdfjs-dist 的 Node 入口可能需要额外配置（canvas 等），建议先用 pdf-parse
4. **并发限制**：DashScope 免费 QPS 可能较低，需压测确认
5. **数据迁移**：前端 localStorage/IndexedDB 已有向量数据，5.12 需迁移

---

## 四、下一步行动

完成本清单后，依次执行：
- 5.1 创建 vector_embeddings 表
- 5.2 kbWorker.js 骨架（轮询+分发+状态更新）
- 5.3 填充解析+嵌入+存储逻辑
- 5.4-5.13 其余任务
