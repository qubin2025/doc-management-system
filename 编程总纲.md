# 工程资料管理系统 — 编程总纲

> 版本 v1.6.0-dev | 2026-05-25 | 全过程工程咨询管理服务平台 | AI底座 · 数据加工中心 · 资产沉淀

---

## 一、项目定位

基于《建筑工程资料管理规程》DB11/T 695-2025，面向建筑工程行业的全过程资料管理与咨询平台。

### 1.1 核心愿景（AI底座）

**AI 大模型作为系统的性能及能力底座。** 有别于传统的流程、填表、审批及文件沉淀输出，本系统以大模型自动化能力及对输入数据分析为基础底座，逐步升级整个系统自动化能力。

**四大原则：**

| 原则 | 说明 |
|---|---|
| **降低输入成本** | 尽量减少输入端输入、维护成本，通过不断丰富、优化输入媒体的可数据化能力和质量 |
| **贯穿AI解析** | 通过贯穿系统各模块的AI解析工具和手段，不断提高数据质量 |
| **形成数据资产** | 形成企业和行业指数级数据指标，按规范形成数据资产 |
| **数据加工中心** | 将系统本身打造成数据加工中心，而非简单的文件存储与流程审批工具 |

### 1.2 开发目标（四阶段路线）

| 阶段 | 名称 | 目标 |
|---|---|---|
| Phase A | AI数据底座 | 文档解析引擎(OCR/PDF/Word/Excel)、多模态数据提取、向量化存储(Embedding)、RAG检索增强生成、语义搜索 |
| Phase B | 数据加工管道 | 结构化提取管道、实体识别与关联、数据标准化层、自动分类(基于附录A)、质量评分引擎、实时校验 |
| Phase C | AI驱动自动化 | AI Agent自主工作流、智能表单自动填充、工作项智能推荐、异常检测与预警、自动报告生成 |
| Phase D | 数据资产化 | 行业指标体系、知识图谱构建、API开放平台、数据资产目录、跨项目洞察、数据产品化输出 |

---

## 二、技术架构

| 层 | 技术 | 说明 |
|---|---|---|
| UI 框架 | React 18 + TypeScript 5 | 全部函数组件 + Hooks |
| 样式 | TailwindCSS 3 | 工具类优先，无自定义CSS(除基础重置) |
| 图标 | lucide-react | 统一图标库 |
| 构建 | Vite 5 | HMR 开发 + 生产构建 |
| 路由 | 自定义 state 路由 | view 状态 + 条件渲染（不用React Router） |
| 状态管理 | useState/useEffect | 根组件提升，不用 Redux/Zustand |
| 存储 | localStorage + API | 离线主存 + 云端备同步 |
| 图表 | draw.io 嵌入 | 时序逻辑图编辑器 |

---

## 三、项目结构

```
src/
├── main.tsx                    # 入口
├── App.tsx                     # 根组件（路由/状态/页面逻辑）
├── index.css                   # Tailwind 指令 + 基础样式
├── types/index.ts              # 所有 TypeScript 接口（14个类型）
├── data/
│   ├── api.ts                  # 后端 API 封装
│   ├── appendixA.ts            # 建筑规程数据
│   ├── appendixA_municipal.ts  # 市政规程数据
│   └── guideModules.ts         # 4章指南模块定义
└── components/
    ├── LoginPage.tsx           # 登录页
    ├── DocumentTable.tsx       # 资料管理主表
    ├── FilterBar.tsx           # 筛选栏
    ├── UploadModal.tsx         # 上传弹窗
    ├── DownloadModal.tsx       # 下载弹窗
    ├── FileListModal.tsx       # 文件管理弹窗
    ├── GuideChapter.tsx        # 指南章节详情
    ├── LogicDiagram.tsx        # 时序逻辑图（draw.io）
    ├── AiChat.tsx              # AI 对话面板
    └── BackupModal.tsx         # 备份弹窗
```

---

## 四、组件编写模板

### 4.1 标准页面组件结构

```tsx
import React, { useState, useEffect, useCallback, useMemo } from 'react';
import { XxxIcon } from 'lucide-react';

interface PageProps {
  data: SomeType;
  onAction: (param: Type) => void;
}

const Page: React.FC<PageProps> = ({ data, onAction }) => {
  // 1. 状态声明
  const [state, setState] = useState<Type>(initVal);
  
  // 2. 派生值（缓存）
  const computed = useMemo(() => { ... }, [deps]);
  
  // 3. 副作用
  useEffect(() => { ... }, [deps]);
  
  // 4. 事件处理
  const handleClick = useCallback(() => { ... }, [deps]);
  
  // 5. 渲染
  return (
    <div className="min-h-screen bg-gray-100">
      <header className="bg-white shadow-sm border-b border-gray-200 sticky top-0 z-30">
        <div className="max-w-7xl mx-auto px-4 py-4">
          <div className="flex items-center justify-between">
            {/* 左侧：Logo + 标题 */}
            <div className="flex items-center gap-3">
              <div className="w-10 h-10 bg-blue-600 flex items-center justify-center rounded-none">
                <span className="text-white font-black text-xs">ZHJK</span>
              </div>
              <h1 className="text-xl font-bold text-gray-800">页面标题</h1>
            </div>
            {/* 右侧：操作按钮组 */}
            <div className="flex items-center gap-3">
              {/* 按钮们 */}
            </div>
          </div>
        </div>
      </header>
      <div className="max-w-7xl mx-auto px-4 py-4">
        {/* 主体内容 */}
      </div>
    </div>
  );
};

export default Page;
```

### 4.2 弹窗组件模板

```tsx
interface ModalProps {
  title: string;
  onClose: () => void;
  onSubmit: (data: Type) => void;
}

const Modal: React.FC<ModalProps> = ({ title, onClose, onSubmit }) => (
  <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
    <div className="bg-white rounded-lg shadow-xl w-full max-w-lg mx-4 max-h-[90vh] flex flex-col">
      {/* 头部 */}
      <div className="flex items-center justify-between p-4 border-b shrink-0">
        <h3 className="text-lg font-semibold">{title}</h3>
        <button onClick={onClose} className="p-1 hover:bg-gray-100 rounded">
          <X className="w-5 h-5" />
        </button>
      </div>
      {/* 内容 */}
      <div className="p-4 overflow-y-auto flex-1">
        {/* 表单字段 */}
      </div>
      {/* 底部 */}
      <div className="flex justify-end gap-3 p-4 border-t bg-gray-50 shrink-0">
        <button onClick={onClose}
          className="px-4 py-2 text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50">
          取消
        </button>
        <button onClick={onSubmit}
          className="px-4 py-2 text-white bg-blue-500 rounded-lg hover:bg-blue-600">
          确认
        </button>
      </div>
    </div>
  </div>
);
```

---

## 五、设计规范速查

### 5.1 颜色体系

| 语义 | Tailwind | HEX |
|---|---|---|
| 主色 | `bg-blue-500` / `text-blue-600` | #3B82F6 |
| 成功 | `bg-green-500` / `text-green-600` | #10B981 |
| 警告 | `bg-yellow-500` / `text-yellow-600` | #EAB308 |
| 危险 | `bg-red-500` / `text-red-600` | #EF4444 |
| 中性 | `bg-gray-100` / `text-gray-500` | #F3F4F6 / #6B7280 |

### 5.2 页面布局

- 主容器：`min-h-screen bg-gray-100`
- 内容区：`max-w-7xl mx-auto px-4 py-4`
- 统计卡片：`grid grid-cols-3 gap-4`
- 卡片：`bg-white rounded-lg shadow-sm p-4 border`
- 页脚：`bg-white border-t border-gray-200 mt-auto`

### 5.3 按钮规范

| 级别 | 类名 |
|---|---|
| 主按钮 | `px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600` |
| 次按钮 | `px-4 py-2 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 text-gray-700` |
| 危险按钮 | `px-2 py-1 bg-red-500 text-white rounded hover:bg-red-600` |
| 图标按钮 | `p-1 bg-blue-500 text-white rounded hover:bg-blue-600` |

### 5.4 徽章/标签

```tsx
// 状态标签
<span className="px-2 py-0.5 text-xs rounded-full bg-green-100 text-green-700">已上线</span>
<span className="px-2 py-0.5 text-xs rounded-full bg-blue-100 text-blue-700">进行中</span>
<span className="px-2 py-0.5 text-xs rounded-full bg-gray-100 text-gray-500">规划中</span>
```

### 5.5 划分卡片

```tsx
<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-5">
  <div className="bg-white rounded-xl shadow-sm p-5 border-2 border-transparent hover:shadow-md hover:-translate-y-1 transition-all duration-200">
    <div className="w-11 h-11 rounded-lg bg-gray-50 flex items-center justify-center mb-3">
      {/* 图标 */}
    </div>
    <h3 className="text-base font-bold text-gray-800">名称</h3>
    <p className="text-xs text-gray-500">描述</p>
  </div>
</div>
```

---

## 六、数据流规范

### 6.1 状态管理模式

```
App.tsx (根组件)
  ├── state + setState (全部全局状态)
  ├── useEffect (持久化 → localStorage)
  ├── useCallback (事件处理，稳定引用)
  └── useMemo (派生计算，避免重复)
      │
      ├── props → 子组件-1
      │    └── onXxx callback → App.tsx 更新状态
      ├── props → 子组件-2
      └── ...
```

### 6.2 存储键名规范

```
doc-system-auth           — 登录状态
doc-mgmt-upload-{标准}     — 上传数据
doc-mgmt-projects-{标准}   — 项目列表
guide-chapter-{章节ID}     — 工作项勾选
guide-chapter-{章节ID}-done — 工作项完成
guide-chapter-{章节ID}-modules — 子模块数据
guide-item-links-{章节ID}  — 连线数据
guide-item-positions-{章节ID} — 节点位置
```

---

## 七、章节颜色体系

| 章节 | 主色 | 辅色 | 深色 |
|---|---|---|---|
| ch1 前期工作 | #3B82F6 (蓝) | #DBEAFE | #1E40AF |
| ch2 招标采购 | #F59E0B (琥珀) | #FEF3C7 | #92400E |
| ch3 工程施工 | #10B981 (翠绿) | #D1FAE5 | #065F46 |
| ch4 竣工验收 | #6366F1 (靛蓝) | #E0E7FF | #3730A3 |

---

## 八、TypeScript 核心类型

```typescript
// 资料项
interface DocumentItem { id, category, subCategory, name, tableCode, archiveUnits }

// 上传记录
interface UploadInfo { fileName, uploadTime, uploader, version, fileData? }

// 筛选选项
interface FilterOptions { category, archiveUnit, searchKeyword }

// 指南工作项
interface GuideWorkItem { id, name, checked, duration?, attachmentFormat?, completedAt?, plannedDate? }

// 指南子模块
interface GuideSubModule { id, name, workItems: GuideWorkItem[] }

// 连线
interface GuideLink { from, to, label?, duration?, startTime?, endTime?, waypoints? }

// 章节
interface GuideChapter { id, number, title, subtitle, subModules, links, forms }
```

---

## 九、经验规则

### ✅ 推荐
- 所有新模块参考 `GuideChapter.tsx` 模板
- 使用 Tailwind 工具类，避免自定义 CSS
- 图标只使用 lucide-react
- Modal 统一使用三段式（头/体/底）
- localStorage + API 双写模式
- 组件行数超过 300 行考虑拆分

### ❌ 避免
- 不使用 React Router
- 不使用 Redux/Zustand/MobX
- 不使用 CSS Modules/styled-components
- 不混用图标库
- 不使用 Class 组件
- 不引入非 MIT 协议的库

---

生成日期：2026-05-08 | 作者：开发团队
