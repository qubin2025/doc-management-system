# 知识库 & GraphRAG 优化 — 开发方案与评估

> 基于《v5.1 知识库&GraphRAG优化需求文档》| 2026-07-30

---

## 一、需求文档评审

### 文档总体评价

需求文档覆盖全面（8个痛点 → 13项需求 → 4个迭代阶段），架构层面（5库体系+混合检索+知识治理）方向正确。但部分需求（BIM联动、KaaS API）在 v5.1 现阶段为超前设计，需按优先级分批落地。

---

## 二、现有能力对照（已建成 vs 文档需求）

| 文档需求 | 当前状态 | 差距 |
|----------|---------|------|
| **5大知识库** | 现有4库（规程/政策/经验/归档），缺「方案模板库」 | 新增1库 + 三维标签 |
| **三维筛选** | KnowledgeBase 仅关键词搜索 | 需生命周期/专业/密级标签体系 |
| **混合检索路由** | 知识编排器有3级降级（RAGFlow→LightRAG→本地） | 缺意图路由 + RRF融合排序 |
| **GraphRAG BFS分发** | 固定2层BFS，无意图路由 | 需合规/方案/精确三类分流 |
| **规范时效管控** | GB/DB标准节点已入库(262节点) | 缺REPLACE_BY边 + 过期拦截 |
| **移动端离线知识** | 离线队列仅用于拍照上传 | 缺知识缓存 + 离线检索 |
| **知识审核工作台** | 无 | 全新页面 |
| **业务-知识双向联动** | 审查结果可手动同步到图谱 | 缺自动沉淀 + 主动推送 |
| **RBAC细粒度权限** | 4角色 + 3权限位 | 缺文档密级 + 项目维度隔离 |
| **知识治理仪表盘** | 无 | 全新页面 |
| **BIM联动/Multi-Agent维护** | 无 | 长期规划 |

---

## 三、方案优劣判定

### 采纳建议（认同且可落地）

| # | 需求 | 采纳 | 理由 |
|---|------|------|------|
| 1 | 5大知识库+三维筛选 | ✅ | 原子化改造，元数据字段扩展即可，不破坏现有结构 |
| 2 | 混合检索意图路由 | ✅ | 在现有知识编排器上叠加路由层，不重写 |
| 3 | 规范时效管控 | ✅ | 已有262标准节点，仅需加边+前端提醒 |
| 4 | 移动端离线知识 | ✅ | 已有IndexedDB+离线队列基础设施 |
| 5 | 知识审核工作台 | ✅ | 独立页面，不侵入现有模块 |
| 6 | 知识运营仪表盘 | ✅ | 独立页面，复用审计日志表 |
| 7 | 业务-知识双向联动 | ✅ | 增量改造，不改核心逻辑 |

### 暂缓建议（方向正确但需延后）

| # | 需求 | 暂缓理由 |
|---|------|---------|
| BIM联动 | BIM解析器/3D渲染是独立子系统，当前无BIM数据源 |
| Multi-Agent知识维护 | 依赖图谱重构完成后再做，否则Agent操作不稳定的图谱 |
| KaaS对外API | 需先完成权限隔离 + 审计日志，否则暴露敏感数据 |
| 项目私有库 | 需PostgreSQL多租户迁移作为前提 |

### 修正建议（文档方案需调整）

| 文档方案 | 问题 | 修正建议 |
|----------|------|---------|
| "全量重建图谱逻辑废弃" | 现有buildGraph()已工作稳定，全量替代风险高 | 保留全量重建作为兜底，增量同步作为默认 |
| "三级审核工作流" | WorkflowBuilder 是通用引擎，知识审核专用工作流过度耦合 | 审核状态机内置 KnowledgeReview.tsx，不依赖 WorkflowBuilder |
| "密级标签：公开/内部/涉密造价" | 涉密造价涉及数据加密存储，工程量大 | 紧急迭代仅做 UI 标记+前端过滤，不做服务端加密 |
| "定时同步北京DB11标准" | 无官方API可拉取标准更新 | 改为人工触发 + 邮件提醒管理员更新 |

---

## 四、分批开发方案

### Phase A — 紧急迭代（1-2轮，本周可交付）

| # | 任务 | 改什么 | 新增/改造 | 风险评估 |
|---|------|--------|----------|---------|
| **A1** | 5大知识库重构 | `KnowledgeBase.tsx` + 后端 filter API | 改造 | 低 — 数据层不变，仅UI+标签 |
| **A2** | 三维筛选标签 | `KnowledgeBase.tsx` 筛选栏 | 改造 | 低 — 纯前端组合筛选 |
| **A3** | 规范时效管控 | Neo4j REPLACE_BY边 + `RegulationsLibrary.tsx` 标红 | 改造 | 低 — 262节点加边，前端CSS |
| **A4** | 移动端规范速查 | `MobileDashboard.tsx` 新增图标 + 简单列表页 | 新增 | 低 — 复用现有 mobileApi |

### Phase B — 中期迭代（3-5轮）

| # | 任务 | 改什么 |
|---|------|--------|
| **B1** | 混合检索路由 | `knowledgeOrchestrator.ts` 意图检测 + `kg_graphrag.js` 路由参数 |
| **B2** | 移动端离线知识缓存 | IndexedDB 存储 + 离线检索 |
| **B3** | 知识审核工作台 | `KnowledgeReview.tsx` 新页面 |
| **B4** | 业务-知识自动沉淀 | 日报/审查/竣工验收 → 自动提取经验 |

### Phase C — 长期迭代（6轮+）

| # | 任务 |
|---|------|
| C1 | 项目私有知识库 |
| C2 | 知识运营仪表盘 |
| C3 | Multi-Agent 知识维护 Agent |
| C4 | 细粒度 RBAC + 审计日志 |

---

## 五、Phase A 详细方案

### A1: 5大知识库 + 三维筛选

```
KnowledgeBase.tsx 改造:
  左侧: 5库折叠菜单 (规程规范/政策法规/项目经验/方案模板/工程归档)
  顶部: 三维筛选栏 (生命周期▼ 专业▼ 密级▼)
  列表: 卡片式展示, 废止标准红框标记

  元数据字段扩展 (所有文档统一):
    lifecycle: '前期'|'招标'|'施工'|'竣工'|'归档'
    profession: '土建'|'机电'|'市政'|'安全'|'造价'|'合同'|'监理'
    security: '公开'|'内部'|'涉密'
    validFrom/validTo: ISO日期 (可选)
    replacedBy: 标准编号 (可选)

  后端:
    GET /api/knowledge/filter?library=X&lifecycle=Y&profession=Z
    参数全部可选, 缺省返回全部
```

### A3: 规范时效管控

```
Neo4j 补充边:
  为已有标准节点添加 REPLACE_BY / SUPERSEDE 关系
  示例: DB11/T695-2017 -[REPLACE_BY]-> DB11/T695-2025

前端:
  RegulationsLibrary.tsx 列表:
    废止标准 → 红色左边框 + "已废止"标签
    悬浮提示: "该标准于2025-01-01废止，替代标准: DB11/T695-2025"
  
  审查页面 ModelStatusBar:
    若检测到引用废止标准 → 黄色警告条

AI 审查增强 (kg_graphrag.js):
  context 端点增加 effectiveNow 参数 → 过滤 validTo < now 的节点
```

---

## 六、防屎山约束（本次开发强制执行）

1. A1 元数据字段用可选属性，不强制所有文档补全 → 向后兼容
2. A3 REPLACE_BY 边入库脚本独立运行，不修改图谱核心逻辑
3. 新增文件 ≤ 250 行 · 新增 any=0 · TSC每步验证
4. 每个 Phase 独立提交 · 每功能一 commit

---

## 七、确认清单

| 确认项 | 说明 |
|--------|------|
| ☐ Phase A 范围 | A1(知识库)+A3(时效管控)+A4(移动端速查), A2(筛选)与A1合并 |
| ☐ 方案模板库 | 作为知识库子分类还是独立页面？建议先作为子分类快速上线 |
| ☐ 混合检索 B1 是否提前 | B1 价值高但工作量大，是否放入 Phase A？建议 Phase B |
| ☐ 修正建议 | 三级审核内置 / 密级UI标记不加密 / DB11改人工触发 |

---

> 确认后开始 Phase A 开发。
